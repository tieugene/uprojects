New app:
* create new app:
mkdir <myapp> (или скопировать существующую папку) - с urls, views, admin, forms, models
* и его шаблоны:
mkdir templates/<myapp> (или скопировать существующее)
* add app to root:
корневые settings, urls
* поправить всё в <apps> (модели особенно)
* и в templates (base.html особенно)
* ./manage.py validate && ./manage.py syncdb && ./manage.py runserver