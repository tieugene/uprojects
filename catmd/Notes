catmd:
	fmt	zip	name			Comments
	b	-?	Commands
	t	-	Container.Contents	Содержит информацию о составе текущей папки
	t	-	Container.Profile
	t	-	Dialog Stream		Диалог (экранная форма)
	b?	-?	Gallery			галерея картинок
	b?	-?	GUIDData		Global Unique Interface ID
						содержит в себе информацию о наследовании файлов конфигурации. На содержание именно этого файла опирается 1с, когда говорит, что загружаемая конфигурация не является прямым потомком текущей.
	t	-	Main MetaData Stream	Структура конфигурации (в базовых версиях и запароленых - закриптован)
	t	+	MD Programm Text	Программный модуль или описания (обычно дополнительно упакованы)
	b	-	Page.X			MXL, nonpacked
	b?	+	__Picture.x		Картинки (8 bytes (DWORD ?, DWORD size) + BMP)
	b?	-?	TagStream		поток с заставкой и информацией об авторе (?)
			?(GUID|TS)		первые четыре байта это количество сохранений конфигурации. При каждом сохранении размер потока увеличивается на 16 байт.

Pascal-like string:
	BYTE num	1 byte
	FF, WORD num	3 bytes
	FF, FF, FF, DWORD num	7 bytes
	(num != FF+)


MMS:
	const, sbcnt: can B addition last folder - from 4 fields
	clndr: can B 2: "HolidaysDef", {4}
	CJParms -> CJParams
	Buh/SbKind - 16 but not 13 items:
	SbKind[13]:
		{
			{"digit", "txt", "0"}*
		}
	TypOpersDef:
		{""[4]}
	CorrProvsDef:
		{""[4]}
	ProvHardFldDef:
		{""[11]}
	OperJournal:	// like Journalisters
		{
			""[9],
			{
				""[4],
				{
					"Refers"
				}
			},
			{
				"JournalFld"
			},
			{
				"Form"
				{""[4]}
			},
		}
	Dialog:
		* Control = Can B 43 or 47 fields.
		* Browser is optional
Gallery:
	BMP, WMF, ICO
Container.Profile:
	расписать
Dialog:
	Frame
	[Browser]
	Controls
	[Ver]
DTD:
	ID:
		digit	no
		_*	yes
		uniq	yes (?)
	IDREF:
		digit	no
		widow	no
		0	no (!) => 0th uidref have to B killed
	xmllint:
		--postvalid?
	! Documents/Document.no == uidref
md2xml:	expantion = x3

MXL-In-Doalog:
	<cp>
			<MoxelName nm="Moxel WorkPlace" .../>
			<MoxelPos nm="3".../>
		x	<Entry nm="1"/>
		x	<MoxelNextMode nm="1".../>
Panel (...FE format):
	DWORD	Sign = 0xFFFFFFFEL
	DWORD	BarQty		Qty of panels
	DWORD	ButQty[BarQty]	Qtys of buttons in each panel
	DWORD	PNQty		Qty of Panel names (== PQty)
	Pstr	PName[PBQty]	Panel names
	DWORD	button[]
	...
Rights:
	---- wrong ! ----
	BYTE	6
	DWORD	0
	---- new ----
	DWORD	6	// signature
	Pstring		// ? some texts
