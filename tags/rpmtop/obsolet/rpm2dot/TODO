. Try graphviz
. try rpm -q... | dot

====Rrm2Dot====
? yum deplist provides whatprovides

rpm -qa --queryformat "%{NAME}"
	<rpm --querytags>
+	N[AME]	rpm name
?	P[ROVIDENAME]
?	PROVIDEVERSION
?	D|REQUIRENAME
?	REQUIRES
?	REQUIREVERSION

	V[ERSION]
	R[REVISION]
	E[POCH]
	C[ONFLICTS?]

rpm -q --requires | --provides

Tests:

$rpm -q --requires yum
`'rpm -qf`			/usr/bin/python
				config(yum) = 3.0.3-1.fc6
pkg -q --provides python	python
rpm -q --provides python	python(abi) = 2.4
pkg -q --provides python-...	python-elementtree
pkg				python-sqlite
pkg+ver				rpm >= 0:4.1.1
pkg				rpm-python
				rpmlib(CompressedFileNames) <= 3.0.4-1
				rpmlib(PayloadFilesHavePrefix) <= 4.0-1
pkg				urlgrabber
pkg				yum-metadata-parser

SUMMARY: check on --provides and -ql

$ rpm -q --provides yum
	config(yum) = 3.0.3-1.fc6
	yum = 3.0.3-1.fc6

So, must check:
* filelist (-1l
* provides (-q --provides)
* requires (-q --requires)
