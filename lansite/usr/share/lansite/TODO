9B02:
= Допуск: =
 * Дублирование
 * вид: Заявление/Допуск/Чужой допуск
 * Сравнение 2-х допусков (любых)
 * Заморозка = readonly + pdf
= Logging: =
 * get all
 * action_type
 * HttpRequest?.META.HTTP_HOST/REMOTE_ADDR/REMOTE_HOST/REMOTE_USER
 * Publish: user, host?
= Option: =
 * find precision printer
====
0. dumpdata
1. Create new models:
	* PermitStatement ()
	* PermitOwn (regno (optional), meeting=>Meeting (optional))
	* PermitAlien(sro=>SRO, regno:str, protono, protodate)
	* SRO (Name, Fullname, regno)
2. Permit
	* mk optional: regno, meeting
	* add type (optional)
3. syncdb, load
4. for p in permit.all:
	* create PermitOwn(regno=regno, meeting=meeting)
	* Permit: type=Own, regno=Null, meeting=Null)
5. dump
6. Tune:
	* Permit:
		* remove regno, meeting
		* type=mandatory
	* PermitOwn:
		* regno=mandatory
		* meeting=mandatory
7. load
== OR ==
0. dumpdata
1. Create new models:
	* PermitType (id, name)
	* PermitStatement ()
	* PermitOwn (regno, meeting=>Meeting)
	* PermitAlien(sro=>SRO, regno:str, protono, protodate)
	* SRO (Name, Fullname, regno)
2. Permit
	* del regno, meeting
	* add type
3. convert data
4. syncdb, load
----
5. Admin
6. Forms
7. ...
