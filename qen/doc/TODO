slStart
	tiStart
		=> done
slStartDone
	Display.slUpdateGUI
	slConnect
		tiLogin
			=> done
slConnectDone
	slUpdateGUI
	slUpdateList()
		tiUpdate
			=> done
slUpdateListDone

*******
slStart
	tiStart
		=> done
slStartDone
	Display.slUpdateGUI
	slUpdateList()
		tiUpdate
			=> done

-------
QTreeWidget twNetworks => QListView lwTasks 

+ de bug
+ filter on offer only
+ Accepting task - now can't accept - not! view only + URL
+ subj as URL - Can't; need at least QLabel, but QTable* and QList can conetns QString only => deside get ID from 1st column
+ Actions: Open (/egroupware/index.php?menuaction=infolog.uiinfolog.edit&info_id=62), Refresh
+ counter in tray (?QGraphicsSimpleTextItem)
* Options
* DoubleClick
[* Option.Browser]
[* db - not wanted]
-------
string	access
	addr
digit	anz_subs
{dig: str}	cat
string	confirm
DateTime	datecompleted
DateTime	datemodified
unicode	des
unicode	from
digit	id
digit	id_parent
digit	link_id
	location
digit	modifier
digit	owner
digit	percent
digit	planned_time
digit	pl_id
digit	price
digit	priority
digit	rights
DateTime	startdate
string	status
unicode	subject
string	type
digit	used_time
